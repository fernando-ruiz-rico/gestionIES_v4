const EscenariosView = { template: `<div class="container-fluid py-4"><div class="row mb-4"><div class="col-12 d-flex justify-content-between align-items-center"><h2><i class="bi bi-building"></i> Escenarios</h2><button class="btn btn-primary" @click="abrirModal()"><i class="bi bi-plus-lg"></i> Nuevo Escenario</button></div></div><div class="row"><div class="col-12"><div class="card shadow-sm"><div class="card-body table-responsive"><table class="table table-hover"><thead><tr><th>ID</th><th>Nombre</th><th class="text-end">Acciones</th></tr></thead><tbody><tr v-for="e in escenarios" :key="e.idEscenario"><td>{{ e.idEscenario }}</td><td>{{ e.nombre }}</td><td class="text-end"><button class="btn btn-sm btn-outline-primary me-1" @click="editar(e)"><i class="bi bi-pencil"></i></button><button class="btn btn-sm btn-outline-danger" @click="eliminar(e)"><i class="bi bi-trash"></i></button></td></tr><tr v-if="!escenarios.length"><td colspan="3" class="text-center text-muted py-4">Sin escenarios</td></tr></tbody></table></div></div></div></div><div class="modal fade" id="modalEscenario" tabindex="-1"><div class="modal-dialog"><div class="modal-content"><div class="modal-header"><h5 class="modal-title">{{ esEdicion ? 'Editar' : 'Nuevo' }} Escenario</h5><button type="button" class="btn-close" data-bs-dismiss="modal"></button></div><div class="modal-body"><form @submit.prevent="guardar"><div class="mb-3"><label class="form-label">Nombre *</label><input type="text" class="form-control" v-model="form.nombre" required></div></form></div><div class="modal-footer"><button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button><button type="button" class="btn btn-primary" @click="guardar">{{ esEdicion ? 'Actualizar' : 'Guardar' }}</button></div></div></div></div></div>`, data() { return { escenarios: [], form: { idEscenario: 0, nombre: '' }, esEdicion: false, modal: null }; }, mounted() { this.cargar(); this.modal = new bootstrap.Modal(document.getElementById('modalEscenario')); }, methods: { async cargar() { this.escenarios = await EscenariosAPI.listar(); }, abrirModal() { this.form = { idEscenario: 0, nombre: '' }; this.esEdicion = false; this.modal.show(); }, editar(e) { this.form = { ...e }; this.esEdicion = true; this.modal.show(); }, async guardar() { const r = await EscenariosAPI.guardar(this.form); if (r.success) { Swal.fire({ icon: 'success', title: 'Éxito', text: r.message, timer: 1500, showConfirmButton: false }); this.modal.hide(); this.cargar(); } else { Swal.fire({ icon: 'error', title: 'Error', text: r.error }); } }, eliminar(e) { Swal.fire({ title: '¿Eliminar?', text: e.nombre, icon: 'warning', showCancelButton: true, confirmButtonText: 'Sí' }).then(async res => { if (res.isConfirmed) { const r = await EscenariosAPI.eliminar(e.idEscenario); if (r.success) { Swal.fire({ icon: 'success', timer: 1500 }); this.cargar(); } else { Swal.fire({ icon: 'error', text: r.error }); } } }); } } };
